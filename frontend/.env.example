# Leave unset in development — Vite proxies /api to http://127.0.0.1:8000,
# so the browser sees a same-origin request and CORS never comes up.
#
# Set this only when the frontend and backend are deployed to different hosts.
# VITE_API_BASE=https://your-backend.onrender.com/api
